'use strict';

var chai = require('chai');
var should = chai.should();
var sinon = require('sinon');

var CSVStream = require('../../../lib/client/streams/csv');

describe('Wallet Client Transaction CSV Stream', function() {
  describe('@constructor', function() {
    it('will create an instance', function() {
    });
    it('will create an instance (without new)', function() {
    });
  });
  describe('#_transactionToCSV', function() {
  });
  describe('#_read', function() {
  });
});
